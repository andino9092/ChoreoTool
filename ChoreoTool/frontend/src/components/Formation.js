import React, {useState, useEffect} from "react";
import Person from "./Person";

export default function Formation(props){
    const [numPeople, spawnPerson] = useState(0);
    const [people, setData] = useState();

    return(
        <div>
            A formation
        </div>
    )
}